# 复制为 .env 并按需修改（服务端不自动读取 .env，请用进程管理器注入）

# 监听端口
PORT=8787

# 监听地址。0.0.0.0 表示允许外部访问；置于 Nginx 之后可改为 127.0.0.1
HOST=0.0.0.0

# SQLite 存档路径
DB_PATH=./data/empire.db

# 令牌密钥。留空则自动生成并写入 ./data/jwt.secret
# 多实例部署时必须显式设置且保持一致
JWT_SECRET=

# 回合间隔（毫秒）。默认 180000 = 3 分钟
TICK_MS=180000
